# 谁是卧底 V8 Clean Start 从零重建版

你要从头重新建，按这个顺序：

## 1. 新建 Supabase 项目

Supabase 新建项目后，进入 SQL Editor。

运行：
database_full_start.sql

这个文件会一次性创建：
- rooms
- players
- votes
- speeches
- Realtime
- 原型版 RLS 权限

注意：database_full_start.sql 里有 drop table。
如果是全新项目没关系；如果旧项目有重要数据，不要直接跑。

## 2. 创建 Edge Function

Supabase 左侧进入 Edge Functions，不要进 SQL Editor。

创建函数名：
generate-words

把这个文件内容粘进去部署：
supabase/functions/generate-words/index.ts

## 3. AI Key 两种方式

方式 A：网页里填
进入游戏首页 AI 设置：
- AI API URL: https://api.openai.com/v1
- AI API Key: 你的 OpenAI API Key
- UID: 空着
- 当前模型: gpt-4o-mini

方式 B：Supabase Secrets
Edge Functions → Secrets 添加：
- AI_API_KEY
- AI_BASE_URL
- AI_MODEL

## 4. 前端部署

把下面三个文件上传 GitHub Pages：
- index.html
- style.css
- main.js

上传前必须先改 main.js 顶部：
SUPABASE_URL
SUPABASE_ANON_KEY

## 5. GitHub Pages

仓库上传文件后：
Settings → Pages → Deploy from a branch → main → /root → Save

等链接生成后打开测试。


## V9 更新

V9 主要修复：
- 手机网页里 Realtime 偶尔不刷新，需要手动刷新才能看到发言/投票/阶段变化。
- 增加 1.8 秒轻量自动同步兜底。
- 切回页面、切换房间分页、提交发言/讨论/投票后会立刻重新拉取数据。
- 数据库结构不变，不需要重新跑 SQL。


## V10 更新

修复手机版输入体验：
- 输入框聚焦后自动滚到屏幕中间，避免被键盘挡住。
- 使用 visualViewport 监听键盘高度，给页面底部自动补空间。
- 强制 input / textarea / select 字号 16px，避免 iOS 自动放大导致错位。
- 手机端顶部房间分页改成横向滚动，键盘弹出时不再挤压错位。
- 不需要重新跑 SQL。


## V11 更新

新增可移动悬浮窗：
- 玩家模式：像微信一样的全局聊天讨论，任意页面都能打开。
- 房主模式：房主可在悬浮窗里一键推进流程，不用反复切房主控制台。
- 悬浮窗可拖动，位置会记住。
- 聊天实时更新，未读消息显示红点。
- 数据库结构沿用 speeches.kind='discussion'，不需要重新跑 SQL。


## V12 更新：出题者规则修正

- 自己想词模式：房主就是出题者/出题主持。
- 出题者知道平民词和卧底词，所以系统会自动让房主不参与本局。
- 自己想词模式至少需要 3 个参与玩家 + 1 个出题主持。
- 如果房主也想参与游戏，请使用 AI 出词模式。
- 不需要重新跑 SQL。


## V13 更新：悬浮窗体验修复

- 用户上滑查看旧聊天时，不再自动跳回底部。
- 点击悬浮按钮时，如果按钮在屏幕下方，聊天窗会自动弹到按钮上方。
- 点击悬浮窗外的空白区域可以关闭。
- 房主打开悬浮窗默认进入“房主操控”，玩家默认进入“玩家聊天”。
- 不需要重新跑 SQL。


## V14 更新

修复：
- 移动悬浮按钮后，悬浮窗会根据按钮位置智能出现在上方/下方，不再像固定位置。
- 房主在房间里修改出词模式时，不会被 1.8 秒自动同步刷回旧值。
- 房主打开悬浮窗强制默认进入“房主操控盘”。
- 不需要重新跑 SQL。


## V15 更新：首页双分区 + 好友邀请

新增：
- 首页底部两个按钮栏：开局 / 我的。
- 开局：创建房间、加入房间放在一起；点击后是跳转式页面，不再直接在下面展开。
- 我的：头像和名字设置、好友码、添加好友、收到的邀请、AI 设置。
- 好友码：每个浏览器本地生成一个好友码。
- 邀请好友：进入房间后可在“玩家”页邀请好友进当前房间。
- 好友收到邀请后，在首页“我的 → 收到的邀请”可以接受，自动填入房间码。
- 需要运行 database_v15_patch.sql。


## V16 更新

修复：
- 悬浮聊天窗在手机上跑到屏幕外的问题，使用 visualViewport 重新限制位置。
- “我的”页面重新整理成更清楚的个人资料 / 好友 / 邀请 / AI 入口。
- 首页文案改成“游戏大厅”，方便后续做多游戏入口。
- 不需要重新跑 SQL；沿用 V15 的 database_v15_patch.sql。


## V17 紧急修复

修复：
- V16 里悬浮窗函数名被重复拼接，导致 main.js 启动失败。
- 表现就是页面能打开，但按钮点不动。
- V17 已修复，替换 index.html / style.css / main.js 后用 ?v=17 打开。
- 不需要重新跑 SQL。


## V18 更新：游戏大厅 + 斗地主

新增：
- 首页改成游戏大厅。
- “谁是卧底”成为游戏大厅里的一个游戏入口。
- 新增“斗地主”基础联机版：
  - 创建/加入斗地主房间
  - 三人房间
  - 发牌
  - 叫地主
  - 地主拿三张底牌
  - 出牌/不要
  - 基础牌型判断：单张、对子、三张、三带一、三带一对、顺子、连对、炸弹、王炸
  - 自动判断地主/农民胜利
- 需要运行 database_v18_ddz_patch.sql。


## V19 更新：斗地主增强

新增：
- 斗地主创建房间可选择三人 / 四人。
- 三人斗地主：一副牌，3 张底牌。
- 四人斗地主：两副牌，8 张底牌，地主对三名农民。
- 牌桌信息栏：显示模式、地主、当前轮到谁。
- 牌桌玩家卡片更像正常牌桌：地主高亮、自己高亮、剩余牌数。
- 新增“提示”按钮：轮到你时自动找一手简单可出的牌。
- 新增更多基础牌型：
  - 四带二
  - 四带两对
  - 飞机
  - 飞机带单
  - 飞机带对
  - 多张炸弹
  - 四王炸
- 需要运行 database_v19_ddz_patch.sql。


## V20 更新：真实牌桌 UI

重做斗地主视觉：
- 牌桌变成绿色桌布风格，不再像普通列表页。
- 玩家围绕牌桌分布：自己在底部，对手在左右/上方。
- 地主、轮到谁、自己都做了明显高亮。
- 手牌改成横向扇形叠牌效果，更像真出牌。
- 出牌按钮变成底部操作条。
- 桌面中心显示模式、地主、当前轮到谁、底牌和上一手牌。
- 不需要重新跑 SQL。


## V21 更新：真实发牌与扑克牌面

新增：
- 点击开始发牌后，进入“洗牌发牌”阶段。
- 牌桌中央出现牌背洗牌、发牌动画。
- 发牌结束后再进入叫地主。
- 手牌改成更像真实扑克：上下角点数、花色、中间花色/大小王。
- 选牌仍然会向上弹起。
- 不需要重新跑 SQL。
